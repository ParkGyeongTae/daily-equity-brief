# Claude Code 모범 사례 (Best Practices)

> 원문: https://code.claude.com/docs/en/best-practices
> 환경 설정부터 병렬 세션 확장까지, Claude Code를 최대한 활용하기 위한 팁과 패턴 정리

Claude Code는 단순한 챗봇이 아니라 **에이전트형 코딩 환경**이다. 질문에 답하고 기다리는 대신, 파일을 읽고 명령을 실행하고 코드를 수정하며 스스로 문제를 해결해 나간다. 사용자는 그 과정을 지켜보거나, 방향을 바꾸거나, 아예 자리를 비울 수도 있다.

따라서 작업 방식이 달라진다. 직접 코드를 작성하고 Claude에게 리뷰를 맡기는 것이 아니라, **원하는 것을 설명하면 Claude가 만드는 방법을 찾아낸다.** 탐색하고, 계획하고, 구현한다.

다만 이런 자율성에는 학습 곡선이 따른다. Claude가 동작하는 제약 조건을 이해해야 한다.

---

## 핵심 제약: 컨텍스트 윈도우

대부분의 모범 사례는 하나의 제약에서 출발한다. **Claude의 컨텍스트 윈도우는 빠르게 차고, 차오를수록 성능이 저하된다.**

- 컨텍스트 윈도우에는 대화 전체가 담긴다: 모든 메시지, Claude가 읽은 모든 파일, 모든 명령 출력.
- 디버깅 세션 하나, 코드베이스 탐색 한 번이 수만 토큰을 소비할 수 있다.
- 컨텍스트가 가득 차면 Claude는 앞선 지시를 "잊거나" 실수가 늘어난다.

**컨텍스트 윈도우는 관리해야 할 가장 중요한 자원이다.**

- 커스텀 스테이터스 라인으로 컨텍스트 사용량을 지속적으로 추적한다.
- `/context` 로 현재 무엇이 로드되어 있는지 확인한다.

---

## 1. Claude에게 스스로 검증할 수단을 줘라

> 💡 Claude가 직접 실행할 수 있는 검사(테스트, 빌드, 비교용 스크린샷)를 제공하라. 이것이 "지켜봐야 하는 세션"과 "자리를 비워도 되는 세션"의 차이를 만든다.

Claude는 작업이 **끝나 보일 때** 멈춘다. 실행 가능한 검사가 없으면 "끝나 보인다"가 유일한 신호이고, 결국 사용자가 검증 루프가 된다. 모든 실수는 사용자가 발견할 때까지 방치된다.

통과/실패를 만들어내는 무언가를 주면 루프가 스스로 닫힌다. Claude가 작업하고 → 검사를 돌리고 → 결과를 읽고 → 통과할 때까지 반복한다.

**검사가 될 수 있는 것**: 테스트 스위트, 빌드 종료 코드, 린터, 출력과 픽스처를 비교하는 스크립트, 디자인과 대조할 브라우저 스크린샷 등.

### 프롬프트 개선 예시

| 전략 | ❌ Before | ✅ After |
|---|---|---|
| **검증 기준 제시** | "이메일 주소를 검증하는 함수를 구현해줘" | "validateEmail 함수를 작성해줘. 테스트 케이스 예시: user@example.com은 true, invalid는 false, user@.com은 false. 구현 후 테스트를 실행해줘" |
| **UI 변경은 시각적으로 검증** | "대시보드를 더 예쁘게 만들어줘" | "[스크린샷 첨부] 이 디자인대로 구현하고, 결과 스크린샷을 찍어 원본과 비교해줘. 차이점을 나열하고 수정해줘" |
| **증상이 아닌 근본 원인 해결** | "빌드가 실패해" | "빌드가 이 에러로 실패해: [에러 붙여넣기]. 수정하고 빌드가 성공하는지 확인해줘. 에러를 억제하지 말고 근본 원인을 해결해줘" |

### 검증을 얼마나 강하게 강제할지

| 수준 | 방법 | 설명 |
|---|---|---|
| **단일 프롬프트** | 같은 메시지에 "검사를 실행하고 반복하라"고 요청 | 오늘 당장 어떤 작업에도 적용 가능 |
| **세션 전체** | `/goal` 조건으로 설정 | 별도 평가자가 매 턴마다 재검사하고, 목표가 해결될 때까지 Claude가 계속 작업 |
| **결정적 게이트** | `Stop` 훅 | 검사를 스크립트로 실행하고, 통과할 때까지 턴 종료를 차단 (8회 연속 차단 시 Claude Code가 훅을 무시하고 종료) |
| **제2의 의견** | 검증용 서브에이전트 / 동적 워크플로 | 작업한 에이전트가 스스로 채점하지 않도록 새로운 모델이 결과를 반박 시도 |

**증거를 요구하라.** "성공했다"는 주장 대신 테스트 출력, 실행한 명령과 그 반환값, 결과 스크린샷을 보여달라고 하라. 증거를 검토하는 편이 직접 재검증하는 것보다 빠르고, 지켜보지 않은 세션에도 유효하다.

---

## 2. 탐색 → 계획 → 구현 순서로

> 💡 조사·계획 단계를 구현과 분리하면 엉뚱한 문제를 푸는 일을 막을 수 있다.

Claude가 곧바로 코딩에 들어가면 **잘못된 문제를 푸는 코드**가 나올 수 있다. 플랜 모드(plan mode)로 탐색과 실행을 분리한다.

### 권장 4단계 워크플로

1. **탐색 (Explore)**
   - `Shift+Tab`을 눌러 상태바에 `⏸ plan mode on`이 뜰 때까지 전환하거나, `claude --permission-mode plan`으로 세션 시작
   - Claude가 변경 없이 파일을 읽고 질문에 답한다
   ```
   /src/auth 를 읽고 세션과 로그인을 어떻게 처리하는지 파악해줘.
   시크릿용 환경 변수를 어떻게 관리하는지도 살펴봐줘.
   ```

2. **계획 (Plan)**
   - 상세한 구현 계획을 요청한다
   ```
   Google OAuth를 추가하고 싶어. 어떤 파일이 바뀌어야 해?
   세션 흐름은 어떻게 돼? 계획을 세워줘.
   ```
   - `Ctrl+G` 로 계획을 텍스트 에디터에서 직접 편집할 수 있다

3. **구현 (Implement)**
   - 계획을 승인하거나 `Shift+Tab`으로 플랜 모드를 빠져나온 뒤 코딩 진행
   ```
   계획대로 OAuth 플로우를 구현해줘. 콜백 핸들러 테스트를 작성하고,
   테스트 스위트를 실행해서 실패를 수정해줘.
   ```

4. **커밋 (Commit)**
   ```
   설명이 담긴 메시지로 커밋하고 PR을 열어줘
   ```

> ⚠️ **플랜 모드는 유용하지만 오버헤드도 있다.**
> 범위가 명확하고 수정이 작은 작업(오타 수정, 로그 한 줄 추가, 변수명 변경)은 바로 시키는 편이 낫다.
> 계획이 가장 유용한 경우: 접근 방식이 불확실할 때, 여러 파일을 수정할 때, 수정 대상 코드에 익숙하지 않을 때.
> **diff를 한 문장으로 설명할 수 있다면 계획 단계는 건너뛰어라.**

---

## 3. 프롬프트에 구체적인 컨텍스트를 담아라

> 💡 지시가 정확할수록 수정 횟수가 줄어든다.

Claude는 의도를 추론할 수 있지만 마음을 읽지는 못한다. 구체적인 파일을 언급하고, 제약을 알려주고, 참고할 패턴을 가리켜라.

| 전략 | ❌ Before | ✅ After |
|---|---|---|
| **작업 범위 지정** | "foo.py 테스트 추가해줘" | "foo.py에 대해 사용자가 로그아웃된 엣지 케이스를 커버하는 테스트를 작성해줘. mock은 쓰지 마." |
| **출처를 가리키기** | "ExecutionFactory API는 왜 이렇게 이상해?" | "ExecutionFactory의 git 히스토리를 훑어보고 이 API가 어떻게 만들어졌는지 요약해줘" |
| **기존 패턴 참조** | "캘린더 위젯 추가해줘" | "홈 페이지의 기존 위젯 구현 방식을 보고 패턴을 파악해줘. HotDogWidget.php가 좋은 예시야. 그 패턴을 따라 월을 선택하고 연도를 앞뒤로 넘길 수 있는 캘린더 위젯을 구현해줘. 코드베이스에 이미 쓰이는 것 외의 라이브러리는 쓰지 말고 처음부터 만들어줘." |
| **증상 기술** | "로그인 버그 고쳐줘" | "세션 타임아웃 후 로그인이 실패한다는 제보가 있어. src/auth/의 인증 흐름, 특히 토큰 갱신을 확인해줘. 먼저 이슈를 재현하는 실패 테스트를 작성한 뒤 수정해줘" |

단, **모호한 프롬프트가 유용할 때도 있다.** 탐색 중이고 방향을 수정할 여유가 있을 때 "이 파일에서 뭘 개선하면 좋을까?" 같은 질문은 미처 생각하지 못한 것을 드러내 준다.

### 풍부한 컨텍스트 제공 방법

- **`@`로 파일 참조** — 코드 위치를 설명하는 대신 `@경로`로 참조하면 Claude가 응답 전에 파일을 읽는다
- **이미지 직접 붙여넣기** — 복사/붙여넣기 또는 드래그 앤 드롭
- **URL 제공** — 문서·API 레퍼런스 링크. 자주 쓰는 도메인은 `/permissions`로 허용 목록에 등록
- **데이터 파이프 입력** — `cat error.log | claude`
- **Claude가 직접 가져오게 하기** — Bash 명령, MCP 도구, 파일 읽기로 스스로 컨텍스트를 확보하도록 지시

---

## 4. 환경을 설정하라

### 효과적인 CLAUDE.md 작성

> 💡 `/init`으로 현재 프로젝트 구조 기반의 CLAUDE.md 초안을 생성한 뒤, 시간을 들여 다듬어라.

CLAUDE.md는 **모든 대화 시작 시 Claude가 읽는 특별한 파일**이다. Bash 명령, 코드 스타일, 워크플로 규칙을 담는다. 코드만으로는 추론할 수 없는 지속적 컨텍스트를 제공한다.

정해진 형식은 없고, **짧고 사람이 읽기 쉽게** 유지하는 것이 핵심이다.

```markdown
# 코드 스타일
- CommonJS(require)가 아닌 ES 모듈(import/export) 문법 사용
- 가능하면 import를 구조 분해 (예: import { foo } from 'bar')

# 워크플로
- 일련의 코드 변경을 마쳤으면 반드시 타입체크할 것
- 성능을 위해 전체 테스트 스위트보다 단일 테스트 실행을 선호
```

- `/context`로 파일이 제대로 로드됐는지 확인한다
- CLAUDE.md는 **매 세션 로드**되므로 광범위하게 적용되는 내용만 넣는다
- 가끔만 필요한 도메인 지식·워크플로는 **스킬(skills)** 로 옮긴다 (필요할 때만 로드되어 대화를 부풀리지 않음)

**간결하게 유지하라.** 각 줄마다 자문하라: *"이 줄을 지우면 Claude가 실수하게 되는가?"* 아니라면 삭제하라. **비대해진 CLAUDE.md는 Claude가 실제 지시를 무시하게 만든다.**

| ✅ 포함할 것 | ❌ 제외할 것 |
|---|---|
| Claude가 추측할 수 없는 Bash 명령 | 코드를 읽으면 알 수 있는 내용 |
| 기본값과 다른 코드 스타일 규칙 | Claude가 이미 아는 표준 언어 관례 |
| 테스트 지침, 선호하는 테스트 러너 | 상세한 API 문서 (링크로 대체) |
| 저장소 예절 (브랜치 명명, PR 규약) | 자주 바뀌는 정보 |
| 프로젝트 고유의 아키텍처 결정 | 긴 설명이나 튜토리얼 |
| 개발 환경 특이사항 (필수 환경 변수) | 파일별 코드베이스 설명 |
| 흔한 함정, 비직관적 동작 | "깔끔한 코드를 작성하라" 같은 자명한 원칙 |

**진단 팁**
- 규칙이 있는데도 Claude가 계속 원치 않는 행동을 한다 → 파일이 너무 길어 규칙이 묻힌 것
- CLAUDE.md에 답이 있는 질문을 Claude가 되묻는다 → 문구가 모호한 것
- **CLAUDE.md를 코드처럼 다뤄라**: 문제가 생기면 리뷰하고, 주기적으로 가지치기하고, Claude의 행동이 실제로 바뀌는지로 변경을 검증하라
- 체크인된 CLAUDE.md는 `/doctor`를 실행하면 코드베이스에서 유추 가능한 내용을 Claude가 삭제 제안해준다
- 특정 지시를 계속 건너뛴다면 **그 줄 하나에만** "IMPORTANT"를 붙여라. 여러 줄을 강조하면 아무것도 두드러지지 않는다
- **git에 체크인해서 팀이 함께 기여하게 하라.** 시간이 지날수록 가치가 복리로 쌓인다
- `@path/to/import` 문법으로 다른 파일을 임포트할 수 있다

### 권한 설정

> 💡 신뢰하는 도구는 `/permissions`로 사전 승인하고, 샌드박스 명령은 `/sandbox`로 확인 없이 실행하게 하라. 직접 승인하고 싶다면 Manual 모드로 전환하라.

- **Pro, Max, Team 플랜**: 대화형 터미널과 VS Code 세션의 기본값은 **auto 모드**. 별도의 분류 모델이 대부분의 동작을 검토하고, 권한 확대·미확인 인프라·악의적 콘텐츠 유발 행동 등 위험해 보이는 것만 차단한다.
- **그 외 플랜**: 기본값은 **Manual 모드**. 파일 쓰기, Bash 명령, MCP 도구 등 시스템을 수정할 수 있는 동작마다 확인을 요청한다. 안전하지만 번거롭고, 열 번째 승인쯤 되면 검토가 아니라 기계적 클릭이 된다.

중단을 줄이는 두 가지 수단 (Manual 모드에 필수, auto 모드에도 적용):

- **권한 허용 목록(allowlist)**: `npm run lint`, `git commit`처럼 안전하다고 아는 도구를 허용
- **샌드박싱**: OS 수준 격리로 파일시스템·네트워크 접근을 제한해, 정해진 경계 안에서 Claude가 더 자유롭게 작업하도록 허용

### CLI 도구 활용

> 💡 외부 서비스와 상호작용할 때는 `gh`, `aws`, `gcloud`, `sentry-cli` 같은 CLI 도구를 쓰도록 지시하라.

- CLI 도구는 외부 서비스와 상호작용하는 **가장 컨텍스트 효율적인 방법**이다
- GitHub을 쓴다면 `gh` CLI를 설치하라. 이슈 생성, PR 열기, 코멘트 읽기에 활용한다. `gh` 없이도 GitHub API를 쓸 수 있지만 미인증 요청은 자주 rate limit에 걸린다
- Claude는 모르는 CLI 도구도 잘 익힌다: `'foo-cli-tool --help'로 foo 도구를 익힌 다음, A, B, C를 해결해줘`

### MCP 서버 연결

> 💡 `claude mcp add`에 서버 이름과 URL/명령을 넘겨 Notion, Figma, 데이터베이스 같은 외부 도구를 연결한다.
> 예: `claude mcp add --transport http notion https://mcp.notion.com/mcp`

MCP 서버로 이슈 트래커의 기능 구현, 데이터베이스 질의, 모니터링 데이터 분석, Figma 디자인 연동, 워크플로 자동화를 요청할 수 있다.

### 훅(Hooks) 설정

> 💡 **예외 없이 매번 일어나야 하는 동작**에는 훅을 사용하라.

훅은 Claude 워크플로의 특정 시점에 스크립트를 자동 실행한다. **CLAUDE.md 지시는 권고적이지만, 훅은 결정적이며 실행을 보장한다.**

- Claude에게 훅 작성을 시킬 수 있다: *"파일 편집마다 eslint를 실행하는 훅을 작성해줘"*, *"migrations 폴더 쓰기를 차단하는 훅을 작성해줘"*
- `.claude/settings.json`을 직접 편집해 수동 설정 가능
- `/hooks`로 설정된 훅을 둘러볼 수 있다

### 스킬(Skills) 만들기

> 💡 `.claude/skills/`에 `SKILL.md` 파일을 만들어 도메인 지식과 재사용 가능한 워크플로를 부여하라.

스킬은 프로젝트·팀·도메인 고유 정보로 Claude의 지식을 확장한다. 관련 상황에서 자동 적용되거나, `/skill-name`으로 직접 호출할 수 있다.

```markdown
<!-- .claude/skills/api-conventions/SKILL.md -->
---
name: api-conventions
description: REST API design conventions for our services
---
# API Conventions
- URL 경로는 kebab-case 사용
- JSON 속성은 camelCase 사용
- 목록 엔드포인트에는 항상 페이지네이션 포함
- API 버전은 URL 경로에 표기 (/v1/, /v2/)
```

직접 호출하는 반복 워크플로도 정의할 수 있다:

```markdown
<!-- .claude/skills/fix-issue/SKILL.md -->
---
name: fix-issue
description: Fix a GitHub issue
disable-model-invocation: true
---
GitHub 이슈를 분석하고 수정한다: $ARGUMENTS

1. `gh issue view`로 이슈 상세 확인
2. 이슈에 기술된 문제 파악
3. 코드베이스에서 관련 파일 검색
4. 수정에 필요한 변경 구현
5. 테스트를 작성·실행해 수정 검증
6. 린팅과 타입 체크 통과 확인
7. 설명이 담긴 커밋 메시지 작성
8. 푸시하고 PR 생성
```

`/fix-issue 1234`로 호출한다. 부수 효과가 있어 수동으로만 트리거하고 싶은 워크플로에는 `disable-model-invocation: true`를 사용한다.

### 커스텀 서브에이전트 만들기

> 💡 `.claude/agents/`에 전문화된 어시스턴트를 정의하면 Claude가 격리된 작업을 위임할 수 있다.

서브에이전트는 **자체 컨텍스트와 자체 허용 도구 집합**으로 실행된다. 많은 파일을 읽거나 전문적 집중이 필요한 작업을 메인 대화를 어지럽히지 않고 처리할 때 유용하다.

```markdown
<!-- .claude/agents/security-reviewer.md -->
---
name: security-reviewer
description: Reviews code for security vulnerabilities
tools: Read, Grep, Glob, Bash
model: opus
---
당신은 시니어 보안 엔지니어다. 다음 항목으로 코드를 리뷰하라:
- 인젝션 취약점 (SQL, XSS, 명령 인젝션)
- 인증·인가 결함
- 코드 내 시크릿/자격 증명
- 안전하지 않은 데이터 처리

구체적인 라인 참조와 수정 제안을 제시하라.
```

명시적으로 지시하라: *"서브에이전트를 써서 이 코드의 보안 이슈를 리뷰해줘."*

### 플러그인 설치

> 💡 `/plugin`으로 마켓플레이스를 둘러보라. 플러그인은 설정 없이 스킬·도구·통합을 추가한다.

플러그인은 스킬, 훅, 서브에이전트, MCP 서버를 하나의 설치 단위로 묶은 것이다(커뮤니티 및 Anthropic 제공). 타입 언어를 쓴다면 **code intelligence 플러그인**을 설치해 정확한 심볼 탐색과 편집 후 자동 에러 감지를 활용하라.

---

## 5. 효과적으로 소통하라

### 코드베이스에 질문하기

> 💡 시니어 엔지니어에게 물어볼 법한 질문을 그대로 Claude에게 하라.

새 코드베이스에 온보딩할 때 Claude Code를 학습·탐색 도구로 쓴다:

- 로깅은 어떻게 동작해?
- 새 API 엔드포인트는 어떻게 만들어?
- `foo.rs` 134번 줄의 `async move { ... }`는 뭘 하는 거야?
- `CustomerOnboardingFlowImpl`은 어떤 엣지 케이스를 처리해?
- 이 코드는 333번 줄에서 왜 `bar()` 대신 `foo()`를 호출해?

특별한 프롬프팅이 필요 없다. 램프업 시간을 줄이고 다른 엔지니어의 부담도 줄여준다.

### Claude가 당신을 인터뷰하게 하라

> 💡 큰 기능은 먼저 Claude가 당신을 인터뷰하게 하라. 최소한의 프롬프트로 시작해 `AskUserQuestion` 도구로 인터뷰를 요청한다.

Claude는 기술 구현, UI/UX, 엣지 케이스, 트레이드오프 등 미처 생각하지 못한 것들을 묻는다.

```
[간단한 설명]을 만들고 싶어. AskUserQuestion 도구를 써서 나를 상세히 인터뷰해줘.

기술 구현, UI/UX, 엣지 케이스, 우려 사항, 트레이드오프에 대해 물어봐.
뻔한 질문은 하지 말고, 내가 고려하지 못했을 어려운 부분을 파고들어줘.

모든 게 다뤄질 때까지 계속 인터뷰하고, 그다음 완전한 스펙을 SPEC.md에 작성해줘.
```

스펙이 완성되면 **새 세션을 시작해 실행**한다. 새 세션은 구현에만 집중된 깨끗한 컨텍스트를 가지고, 참조할 문서화된 스펙도 있다.

가장 유용한 스펙은 **자기 완결적**이다: 관련 파일과 인터페이스를 명시하고, 범위 밖의 것을 밝히고, 기능이 동작함을 증명하는 종단 간(end-to-end) 검증 단계로 끝맺는다. **스펙을 정밀하게 다듬는 시간이 구현을 지켜보는 시간보다 더 큰 보상을 준다.**

---

## 6. 세션을 관리하라

대화는 지속적이고 되돌릴 수 있다. 이를 적극 활용하라.

### 빠르고 자주 방향을 수정하라

> 💡 궤도를 벗어난다고 느끼는 즉시 수정하라.

가장 좋은 결과는 **촘촘한 피드백 루프**에서 나온다. Claude가 첫 시도에 완벽히 해결하는 경우도 있지만, 대개는 빠르게 교정하는 편이 더 나은 해법을 더 빨리 만든다.

| 조작 | 효과 |
|---|---|
| `Esc` | 동작 중인 Claude를 중단. 컨텍스트는 보존되어 방향 전환 가능 |
| `Esc + Esc` 또는 `/rewind` | 되감기 메뉴를 열어 이전 대화·코드 상태로 복원하거나, 선택한 메시지부터 요약 |
| `"Undo that"` | Claude가 자신의 변경을 되돌리게 함 |
| `/clear` | 관련 없는 작업 사이에 컨텍스트 초기화 |

**한 세션에서 같은 문제로 두 번 넘게 교정했다면**, 컨텍스트가 실패한 접근들로 어지러워진 상태다. `/clear` 후 배운 것을 반영한 더 구체적인 프롬프트로 새로 시작하라. **깨끗한 세션 + 더 나은 프롬프트는 거의 항상 교정이 쌓인 긴 세션을 능가한다.**

### 컨텍스트를 적극적으로 관리하라

> 💡 관련 없는 작업 사이에는 `/clear`로 컨텍스트를 리셋하라.

Claude Code는 컨텍스트 한계에 다가가면 대화 기록을 자동 압축(compaction)하며, 중요한 코드와 결정을 보존하면서 공간을 확보한다.

- 작업 사이에 `/clear`를 **자주** 사용해 컨텍스트 윈도우를 완전히 초기화
- 자동 압축 시 Claude는 코드 패턴, 파일 상태, 핵심 결정 등 중요한 것을 요약
- 더 세밀한 제어: `/compact <지시>` (예: `/compact API 변경에 집중해줘`)
- 대화의 **일부만** 압축하려면 `Esc + Esc` 또는 `/rewind` → 메시지 체크포인트 선택 → **Summarize from here**(그 지점 이후를 압축, 이전 컨텍스트는 그대로) 또는 **Summarize up to here**(이전을 압축, 최근은 온전히 유지)
- CLAUDE.md에 압축 동작을 커스터마이즈: `"압축할 때 수정된 파일 전체 목록과 테스트 명령을 항상 보존하라"`
- 컨텍스트에 남길 필요 없는 질문은 `/btw` 사용. 답변이 대화 기록에 들어가지 않아 컨텍스트를 늘리지 않고 확인 가능

### 조사에는 서브에이전트를 써라

> 💡 `"서브에이전트를 써서 X를 조사해줘"`로 리서치를 위임하라. 별도 컨텍스트에서 탐색하므로 메인 대화는 구현에 집중된 상태로 유지된다.

컨텍스트가 근본적 제약이므로, 리서치는 컨텍스트 밖으로 빼라. Claude가 코드베이스를 조사하면 많은 파일을 읽고 그 전부가 컨텍스트를 소비한다. 서브에이전트는 별도 컨텍스트 윈도우에서 실행되어 요약만 보고한다.

```
서브에이전트를 써서 우리 인증 시스템이 토큰 갱신을 어떻게 처리하는지,
재사용할 만한 기존 OAuth 유틸리티가 있는지 조사해줘.
```

구현 후 검증에도 서브에이전트를 쓸 수 있다.

### 체크포인트로 되감기

> 💡 턴을 시작하는 모든 프롬프트가 체크포인트를 만든다. 대화, 코드, 또는 둘 다를 이전 체크포인트로 복원할 수 있다.

- Claude는 변경 전 파일을 자동 스냅샷하여 체크포인트가 복원할 수 있게 한다
- `Escape` 더블 탭 또는 `/rewind`로 되감기 메뉴를 연다
- 대화만 복원, 코드만 복원, 둘 다 복원, 선택 메시지부터 요약 중 선택 가능
- 매 수를 신중히 계획하는 대신 **위험한 시도를 해보라고 시키고, 안 되면 되감아 다른 접근을 택할 수 있다**
- 체크포인트는 대화와 함께 저장되므로 터미널을 닫고 나중에 세션을 재개해도 되감기가 가능하다

> ⚠️ **주의**: 체크포인트는 Claude의 파일 편집 도구를 통한 변경만 추적한다. Bash 명령이나 외부 프로세스로 만든 변경은 캡처되지 않는다. **git을 대체하지 않는다.**

### 대화 재개

> 💡 `/rename`으로 세션에 이름을 붙이고 브랜치처럼 다뤄라. 작업 흐름마다 고유한 지속 컨텍스트를 갖는다.

- 대화는 로컬에 저장되므로 여러 번에 나눠 하는 작업도 컨텍스트를 다시 설명할 필요가 없다
- `claude --continue` : 하던 곳에서 이어서
- `claude --resume` : 목록에서 선택
- `oauth-migration` 같은 설명적 이름을 붙여두면 나중에 찾기 쉽다

---

## 7. 자동화하고 확장하라

한 개의 Claude를 능숙하게 다루게 됐다면, 병렬 세션·비대화형 모드·팬아웃 패턴으로 산출량을 배가하라.

### 비대화형(non-interactive) 모드

> 💡 CI, pre-commit 훅, 스크립트에서 `claude -p "prompt"`를 사용하라. 스트리밍 JSON 출력은 `--output-format stream-json --verbose`.

`--no-session-persistence`를 넘기지 않는 한 실행은 재개 가능한 세션을 생성한다.

```bash
# 단발성 질의
claude -p "이 프로젝트가 무엇을 하는지 설명해줘"

# 스크립트용 구조화 출력
claude -p "모든 API 엔드포인트를 나열해줘" --output-format json

# 실시간 처리용 스트리밍
claude -p "이 로그 파일을 분석해줘" --output-format stream-json --verbose
```

- 첫 번째: 일반 텍스트 출력
- `json`: `result` 필드를 가진 단일 JSON 객체 반환
- `stream-json`: 줄마다 하나의 JSON 객체 출력, init 이벤트로 시작

### 여러 Claude 세션 동시 실행

> 💡 병렬 세션으로 개발 속도를 높이고, 격리된 실험을 하고, 복잡한 워크플로를 시작하라.

조정 부담에 맞는 방식을 고르고, 세션 간 정보 전달이 필요하면 메시징을 추가한다:

| 방식 | 설명 |
|---|---|
| **Worktrees** | 격리된 git 체크아웃에서 각각 CLI 세션 실행 → 편집 충돌 없음 |
| **Cross-session messaging** | 직접 실행한 세션들이 서로에게 발견 사항을 전달 |
| **데스크톱 앱** | 여러 로컬 세션을 시각적으로 관리, 각각 자체 worktree 사용 가능 |
| **클라우드에서 실행** | Anthropic 관리 인프라에서 세션 실행 |
| **Agent view** (리서치 프리뷰) | `claude agents`로 백그라운드 실행 세션을 디스패치하고 한 화면에서 관찰 |
| **Agent teams** (실험적, 기본 비활성) | 공유 태스크·메시징·팀 리드를 갖춘 다중 세션 자동 조정 |

병렬화 외에도 **품질 중심 워크플로**가 가능하다. 새 컨텍스트는 코드 리뷰를 개선한다. Claude가 방금 작성한 코드에 편향되지 않기 때문이다.

**Writer/Reviewer 패턴 예시**

| 세션 A (Writer) | 세션 B (Reviewer) |
|---|---|
| `API 엔드포인트용 rate limiter를 구현해줘` | |
| | `@src/middleware/rateLimiter.ts 의 rate limiter 구현을 리뷰해줘. 엣지 케이스, 레이스 컨디션, 기존 미들웨어 패턴과의 일관성을 봐줘.` |
| `리뷰 피드백이야: [세션 B 출력]. 이 이슈들을 해결해줘.` | |

테스트로도 비슷하게 할 수 있다. 한 Claude가 테스트를 작성하고, 다른 Claude가 이를 통과시키는 코드를 작성한다.

### 파일 단위로 팬아웃(fan out)

> 💡 작업마다 `claude -p`를 호출하는 루프를 돌려라. 배치 작업에서는 `--allowedTools`로 권한 범위를 제한하라.

대규모 마이그레이션·분석에는 여러 병렬 Claude 호출로 작업을 분산한다. git 저장소에서 `/batch <지시>`를 실행하면 Claude가 변경을 5~30개 서브에이전트로 쪼갠다. 각 서브에이전트는 자체 worktree에서 작업하고 PR을 연다.

직접 스크립트로 팬아웃하려면 `claude -p` 루프:

1. **작업 목록 생성**
   ```
   마이그레이션이 필요한 2,000개의 Python 파일을 모두 나열해 files.txt에 저장해줘
   ```
2. **목록을 순회하는 스크립트 작성**
   ```bash
   for file in $(cat files.txt); do
     claude -p "Migrate $file from Python 2 to Python 3. Return OK or FAIL." \
       --allowedTools "Edit,Bash(git commit *)"
   done
   ```
3. **몇 개로 테스트한 뒤 전체 실행**
   - 처음 2~3개 파일에서 잘못된 점을 바탕으로 프롬프트를 다듬고 전체 집합에 실행
   - `--allowedTools`는 Claude가 할 수 있는 일을 제한하며, 무인 실행 시 특히 중요하다

기존 데이터/처리 파이프라인에도 통합할 수 있다:

```bash
claude -p "<your prompt>" --output-format json | your_command
```

### auto 모드로 자율 실행

백그라운드 안전 검사를 곁들인 중단 없는 실행에는 auto 모드를 사용한다. 분류 모델이 명령 실행 전에 검토하여 권한 확대, 미확인 인프라, 악의적 콘텐츠 유발 동작을 차단하면서 일상적 작업은 확인 없이 진행시킨다.

```bash
claude --permission-mode auto -p "fix all lint errors"
```

`-p` 플래그를 쓴 비대화형 실행에서 분류기가 반복적으로 동작을 차단해도 Claude Code는 실행을 중단하지 않는다.

### 적대적(adversarial) 리뷰 단계를 추가하라

> 💡 작업을 완료로 간주하기 전에, 서브에이전트가 새 컨텍스트에서 diff를 리뷰하고 빠진 부분을 보고하게 하라.

Claude가 무인으로 오래 작업할수록 완료 판정 전의 **독립적 검사**가 중요해진다. 새 서브에이전트 컨텍스트의 리뷰어는 변경을 만들어낸 추론 과정이 아니라 **diff와 주어진 기준만** 보므로 결과를 독자적으로 평가한다.

- 정확성 검사: 번들된 `/code-review` 스킬 실행 → 현재 diff의 버그를 새 서브에이전트에서 리뷰하고 결과를 세션에 반환
- 계획 대비 검사: 리뷰 프롬프트를 직접 작성. 검사 대상, 대조할 계획, 무엇을 발견 사항으로 볼지를 명시

```
서브에이전트를 써서 rate limiter diff를 PLAN.md와 대조해 리뷰해줘.
모든 요구사항이 구현됐는지, 나열된 엣지 케이스에 테스트가 있는지,
작업 범위 밖의 것이 바뀌지 않았는지 확인해줘. 스타일 취향이 아니라 빠진 것을 보고해줘.
```

리뷰어가 서브에이전트로 실행되므로, 구현 세션이 빠진 부분을 직접 전달받아 수정하고 재리뷰할 수 있다. 창을 오가며 결과를 복사할 필요가 없다.

> ⚠️ **주의**: 빠진 것을 찾으라고 지시받은 리뷰어는 작업이 견실해도 대개 뭔가를 보고한다. 그게 시킨 일이기 때문이다. 모든 지적을 좇으면 **과잉 엔지니어링**(불필요한 추상화 계층, 방어적 코드, 발생할 수 없는 케이스의 테스트)으로 이어진다. 정확성이나 명시된 요구사항에 영향을 주는 것만 표시하도록 지시하고, 나머지는 선택 사항으로 취급하라.

---

## 8. 흔한 실패 패턴 피하기

| 실패 패턴 | 설명 | 해결책 |
|---|---|---|
| **잡동사니 세션 (kitchen sink)** | 한 작업으로 시작해 무관한 질문을 하고 다시 첫 작업으로 돌아온다. 컨텍스트가 무관한 정보로 가득 찬다 | 관련 없는 작업 사이에 `/clear` |
| **반복 교정** | Claude가 틀리고, 교정해도 여전히 틀리고, 또 교정한다. 컨텍스트가 실패한 접근으로 오염된다 | 두 번 실패하면 `/clear` 후 배운 것을 반영한 더 나은 초기 프롬프트 작성 |
| **과다 명세된 CLAUDE.md** | 너무 길어서 중요한 규칙이 소음에 묻히고 Claude가 절반을 무시한다 | 가차 없이 가지치기. 지시 없이도 이미 잘한다면 삭제하거나 훅으로 전환 |
| **신뢰 후 검증 공백** | 그럴듯해 보이지만 엣지 케이스를 처리하지 못하는 구현이 나온다 | 항상 검증 수단(테스트, 스크립트, 스크린샷)을 제공. **검증할 수 없으면 배포하지 마라** |
| **무한 탐색** | 범위 없이 "조사해줘"라고 시키면 수백 개 파일을 읽어 컨텍스트를 채운다 | 조사 범위를 좁게 지정하거나 서브에이전트를 써서 메인 컨텍스트를 소모하지 않게 하기 |

---

## 9. 직관을 길러라

이 가이드의 패턴은 불변의 법칙이 아니다. 일반적으로 잘 통하는 **출발점**일 뿐, 모든 상황에 최적은 아니다.

- 하나의 복잡한 문제에 깊이 들어가 있고 히스토리가 가치 있다면 **컨텍스트를 쌓아두는 게 맞을 때도 있다**
- 탐색적 작업이라면 **계획을 건너뛰고 Claude가 알아서 하게 두는 게 맞을 때도 있다**
- 문제를 제약하기 전에 Claude가 어떻게 해석하는지 보고 싶다면 **모호한 프롬프트가 정확히 옳을 때도 있다**

**무엇이 통하는지 관찰하라.** Claude가 훌륭한 결과를 냈을 때 무엇을 했는지 살펴라 — 프롬프트 구조, 제공한 컨텍스트, 사용한 모드. Claude가 헤맬 때는 왜인지 물어라 — 컨텍스트가 너무 시끄러웠나? 프롬프트가 너무 모호했나? 작업이 한 번에 처리하기엔 너무 컸나?

시간이 지나면 어떤 가이드도 담을 수 없는 직관이 생긴다. 언제 구체적이어야 하고 언제 열어두어야 하는지, 언제 계획하고 언제 탐색해야 하는지, 언제 컨텍스트를 비우고 언제 쌓아야 하는지 알게 된다.

---

## 관련 자료

- [How Claude Code works](https://code.claude.com/docs/en/how-claude-code-works) — 에이전트 루프, 도구, 컨텍스트 관리
- [Extend Claude Code](https://code.claude.com/docs/en/features-overview) — 스킬, 훅, MCP, 서브에이전트, 플러그인
- [Common workflows](https://code.claude.com/docs/en/common-workflows) — 디버깅, 테스트, PR 등 단계별 레시피
- [CLAUDE.md](https://code.claude.com/docs/en/memory) — 프로젝트 관례와 지속 컨텍스트 저장
