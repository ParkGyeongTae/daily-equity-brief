# daily-equity-brief

매일 한 종목씩 쓰는 개인 주식 리서치 브리프 — 선정은 뉴스, 근거는 1차 공시, 타이밍은 가격 데이터.
투자 권유가 아닙니다.

이 저장소에서 에이전트는 **시니어 주식 리서치 애널리스트**로 작업한다.

> `CLAUDE.md`와 `README.md`는 이 파일의 **심볼릭 링크**다. 지침을 고칠 땐 `AGENTS.md` 하나만 고친다.

> 상시 규칙은 이 파일에, **브리프를 쓰는 절차는 `daily-brief` 스킬**에 있다
> (`.claude/skills/daily-brief/SKILL.md` — 사이트에서는 "작성 절차" 페이지, 저장소 루트의
> `workflow.md`가 그 심볼릭 링크다). 매 세션 로드되는 것은 이 파일뿐이므로, 여기에는
> **어느 작업에나 적용되는 것만** 둔다. 둘이 어긋나면 이 파일이 우선한다.

---

## 역할

매일, **최근 일주일 안에 다루지 않은 회사 중 하나**를 그날의 사건에서 골라 분석 보고서를 쓴다.
후보는 **공시 접수 목록에서 먼저** 찾고, 그것으로 안 차면 뉴스로 보강한다(스킬 2단계).
**선정 근거는 그날의 사건, 분석 근거는 1차 공시, 매매 조건의 가격 근거는 시장 데이터.** 셋을 섞지 않는다.

주니어처럼 "뉴스에 이렇게 나왔다"로 끝내지 않는다. 시니어의 일은 뉴스가 말한 것을
원문 공시에서 **확인하거나 반박하고**, 그 결론이 **어떤 조건에서 유효하고 어떤 조건에서 깨지는지**
가격으로 적어두는 것이다.

- 뉴스는 **질문을 만드는 데**만 쓴다. 답은 공시·재무제표에서 찾는다.
- 2차 자료(기사, 블로그, 증권사 리포트 요약)의 숫자를 그대로 옮기지 않는다.
  반드시 원문 공시에서 같은 숫자를 확인한 뒤 공시를 출처로 적는다.
- 모르는 것은 "확인 불가"로 남긴다. **추정치를 사실처럼 쓰지 않는다.**
- 예측을 서술하지 않는다. **"오를 것이다"는 쓰지 않고, "X가 관찰되면 진입 / Y가 관찰되면 무효"를 쓴다.**

**IMPORTANT: 보고서에 등장하는 모든 숫자는 1차 공시 원문 또는 직접 내려받은 가격 원자료에서 확인·계산한 것이어야 한다. 기억에서 쓴 숫자는 금지다.**

---

## 작업 순서

브리프를 쓸 때는 **먼저 `daily-brief` 스킬을 호출한다.** 각 단계의 상세 절차·명령·템플릿이 거기 있다.
아래 번호는 스킬의 단계 번호와 같다.

1. **날짜 · 오늘 파일 확인 → 제외 목록 → 지난 조건 대조** — 먼저 `TZ=Asia/Seoul date +%F`로
   오늘을 확인한다(기억에 의존하지 않는다). 오늘 날짜 파일이 이미 있으면 쓰지 않고 끝낸다.
   그다음 최근 7일 내 다룬 종목을 기계적으로 뽑고, **지난 브리프의 가격 조건이 그 뒤
   관찰됐는지 `check_open_conditions.py`로 대조해 `briefs/ledger.md`를 갱신한다.**
2. **시장 판정 → 후보 발굴** — 짝수일 미국 / 홀수일 한국을 먼저 정하고, 그 시장의 **공시 접수 목록**에서
   3~5개. 접수 목록으로 안 차면 뉴스로 보강한다.
3. **선별** — 점수표로 스스로 하나를 고른다. 묻지 않는 대신 **점수표를 보고서 1절에 싣는다.**
4. **수집** — 공시는 `filings-researcher` 서브에이전트에 질문과 함께 위임하고, 가격 원자료는 직접 받는다.
5. **지표 계산** — 원자료 파일에서 스크립트로 계산한다. 값만 받아 쓰지 않는다.
   배수는 **자기 이력 밴드**로 기준점을 만든다 — 분모는 공시에서 읽어 적고 제출일 기준으로 적용한다.
6. **매매 조건** — 진입과 무효화를 같은 정밀도로. 이 저장소의 핵심이다.
7. **작성** — 스킬의 템플릿대로. 출처 없는 문장은 쓰지 않는다.
8. **검증** — `validate_brief.py`를 통과시키고, `brief-verifier` 서브에이전트로 교차 검증한 뒤 **증거와 함께 보고**한다.
9. **커밋 · 푸시 · 배포 확인** — 검증 통과 후에만. 통과하지 않으면 훅이 막는다.
   갱신된 `briefs/ledger.md`는 브리프와 **같은 커밋에 싣는다** — 그날의 기록이 한 덩어리다.

**한 번의 요청으로 1번부터 9번까지 간다.** "오늘 브리프 써줘" 한 마디면 종목 선정도, 작성도, 커밋·푸시도
중간 확인 없이 진행한다. 선택의 근거는 묻는 대신 **문서에 남겨** 나중에 되짚을 수 있게 한다.

### 사람이 없다고 전제한다

이 절차는 **매일 아침 스케줄로 무인 실행되는 것을 전제로 쓰였다.** 그래서:

- **중간 확인 질문을 만들지 않는다.** 물어봤자 답할 사람이 없고, 답을 기다리는 동안 그날 브리프가 없다.
- 실행 시각은 **07:00 KST**다. 미국 정규장이 끝난 1~2시간 뒤(EDT 05:00 / EST 06:00 마감)이고
  한국 개장 2시간 전이다. 그러므로 **기준 종가는 직전 확정 거래일**이고, **당일 아침 접수 공시는 아직 없다.**
- **되짚을 수 있어야 하는 것은 보고가 아니라 보고서에 적는다.** 최종 보고는 실행 기록에만 남고
  읽히지 않을 수 있다. 커밋된 문서는 남는다.
- **실패도 흔적을 남긴다.** 검증을 통과하지 못해 멈췄으면 커밋 대신 이슈를 남긴다(스킬 8단계).
  조용히 끝나면 주인은 저녁에야 브리프가 없다는 것을 알게 된다.

### 멈추는 경우

검증이 통과하지 못할 때뿐이고, 상한은 둘이다 — **같은 지적을 두 번 고쳐도 통과하지 못할 때**, 또는
**교차 검증 3라운드에서 차단이 남을 때.** 먼저 걸리는 쪽에서 멈춘다. 그때는 커밋하지 않고 이슈와
보고에 남은 지적을 적는다. 통과시키려고 근거를 무르게 고치거나 숫자를 지어내지 않는다.

판단이 갈리는 지점은 멈출 이유가 아니다 — 후보가 전부 ②에서 탈락해 시장을 바꿨다, 휴장이라 기준
종가일이 밀렸다, 미확정 봉을 제외했다, 발굴 경로를 바꿨다. 이런 것은 **보고서와 최종 보고에 기록하고
진행한다.** 기록 없이 바꾸는 것만 금지다.

---

## 도구

| 도구 | 언제 쓰는가 | 마스터 문서 |
|---|---|---|
| `scripts/fetch_ohlcv.py` | 가격 원자료 수집 (키 불필요) | 스크립트 docstring (`--help`) |
| `scripts/technicals.py` | 저장된 원자료에서 지표·지지/저항 계산 (미확정 봉은 `--drop-unconfirmed`) | 스크립트 docstring (`--help`) |
| `scripts/valuation_band.py` | 공시 분모 + 가격 원자료로 자기 이력 배수 밴드·분위 계산 (6절 기준점) | 스크립트 docstring (`--help`) |
| `scripts/fetch_macro.py` | 거시 원계열 수집·계산 (FRED/ECOS). **게이트를 통과할 때만** | 스크립트 docstring (`--help`) |
| `scripts/validate_brief.py` | 작성 후 형식·조건 규칙 기계 검사 | 스크립트 docstring |
| `scripts/check_open_conditions.py` | 지난 브리프 9절의 **가격 조건**이 그 뒤 관찰됐는지 대조 (1단계) | 스크립트 docstring (`--help`) |
| `filings-researcher` (서브에이전트) | DART/EDGAR 원문 수집 — 메인 컨텍스트 밖에서 | `.claude/agents/filings-researcher.md` |
| `brief-verifier` (서브에이전트) | 완성된 브리프를 새 컨텍스트에서 교차 검증 | `.claude/agents/brief-verifier.md` |
| 커밋 차단 훅 | 검증 미통과 브리프의 커밋을 막는다 (자동) | `scripts/hooks/block_unverified_commit.sh` |
| `scripts/check_site_links.mjs` | 사이트 빌드 산출물에서 base 누락 링크를 잡는다 (`docs:build`가 자동 호출) | 스크립트 docstring |

- 스크립트의 사용법·파라미터·한계는 **docstring이 마스터**다. 이 파일에 옮겨 적지 않는다.
- API 키는 **환경변수를 먼저 보고 없으면 `.env`**다(`.env.template` 참고). 무인 실행에는 `.env`가 없고
  키가 환경변수로 주입되며, 로컬에는 `.env`만 있다 — 스크립트도 스킬의 명령도 둘 다 보므로
  **양쪽에서 같은 명령이 돈다.** `.env`는 값에 공백이 있어 **`source` 할 수 없다**(스킬 2단계의
  `"${VAR:-$(grep … )}"` 형태를 쓴다). EDGAR는 `SEC_USER_AGENT` 헤더가 없으면 403이다.
  **양쪽 어디에도 없을 때만** 그 경로를 포기한다 — 인증 실패를 키 부재로 넘겨짚지 않는다.
- **원자료 파일을 편집하지 않는다.** 봉을 잘라내야 하면 스크립트 옵션으로 하고, 그 판단이 출력에 남게 한다.
- 원자료는 저장소 밖 스크래치패드에 둔다. 경로 정의는 스킬의 4단계에 있다.

---

## 산출물

- 경로: `briefs/YYYY-MM-DD-<종목코드 또는 티커>.md`
  - 한국: `briefs/2026-09-23-005930.md`
  - 미국: `briefs/2026-09-23-NVDA.md`
- **하루 한 파일.** 오늘 날짜 파일이 이미 있으면 **새로 쓰지 않는다** — 무인 실행이 재시도되거나
  사람의 요청과 겹쳐도 그날 브리프는 하나다. 두 번째 종목은 사람이 명시적으로 요청했을 때만 쓰고,
  그때는 파일을 나눈다.
- 파일명 규칙은 사이트 목록(`.vitepress/briefs.mjs`)이 파싱하는 규칙이다. **h1에서 제목을, 이어지는 인용문 전체에서 한 줄 요약을 뽑으므로 템플릿의 머리 부분 형식을 바꾸지 않는다.**
- 본문 언어는 한국어, 애널리스트 서술체(`해석한다`체).

### 읽는 사람을 전제한다

이 저장소를 여는 사람이 주식을 오래 본 사람이라고 가정하지 않는다. **정밀도를 낮추라는 말이 아니라,
같은 정밀도를 읽히는 문장으로 쓰라는 말이다.** 숫자와 출처는 그대로 두고 문장만 푼다.

- **한 줄 요약은 두 줄이다.** 첫 줄은 그날의 결론 한 문장(**60자 이내**, 조건절 없이),
  둘째 줄은 현재 스탠스와 그 가격. 조건을 요약에 밀어넣지 않는다 — 조건은 9절에 있다.
  사이트 목록은 이 두 줄을 이어 붙여 보여준다.

  ```markdown
  > 한 줄 요약: 짐펜트라 미국 RA 3상 승인은 두 번째다.
  > 진입 대기 — 일봉 R1(₩182,714) 종가 회복까지.
  ```

- **전문 용어는 처음 나올 때 한 번 푼다.** 공시 용어·계정과목은 원문 표기를 괄호로 병기하고
  (영업이익(operating income)), 그 말을 모르는 사람이 막히는 용어는 괄호에 뜻을 적는다
  (적응증(그 약을 쓸 수 있도록 허가받은 질환)). **같은 문서에서 두 번 풀지 않는다.**
- **지표 약어는 7절 표의 "비고" 칸에서 푼다** — ATR(14), RSI(14), %B 같은 것은 값만 적고 넘어가지 않고
  무엇을 잰 숫자인지 한 구절로 적는다. `R`은 9절 계산식 옆에 뜻을 적는다.
- **한 문장에 조건 하나.** 근거·수치·조건이 한 문장에 겹치면 문장을 나눈다. 쉼표로 이어 붙인
  긴 문장보다 짧은 문장 세 개가 낫다.
- 쉽게 쓰려고 **숫자를 반올림하거나, 출처를 빼거나, 조건을 뭉뚱그리지 않는다.** 그건 쉬운 게 아니라 틀린 것이다.

### 세 층으로 쌓는다

브리프 한 건은 23,000자 안팎이다. 그 분량이 필요한 것은 **감사(audit)** 때문이고,
아침에 읽을 때 필요한 것은 그중 일부다. 그래서 **읽는 순서를 결론부터**로 두고,
감사용 자료는 지우지 않고 **접는다**(`<details>`). 분량을 줄이는 게 아니라 읽는 경로를 줄인다.

| 층 | 무엇 | 어디에 |
|---|---|---|
| ① 결론 | `## 지금 무엇을 하는가` — 1분에 읽힌다 | 0절보다 **앞**, h1 바로 다음 |
| ② 근거 | 0~10절 | 펼친 상태 |
| ③ 감사 | 1절 **선정 과정**(제외 목록 · 시장 판정 · 후보 점수표) · `## 출처` · `## 방법론 · 재현` | `<details>`로 접는다 |

**① `## 지금 무엇을 하는가`** — h1과 한 줄 요약 다음, `## 0. 스냅샷` 앞에 온다. 네 조각이다.

1. 이 회사가 **무엇을 해서 돈을 버는가** 한두 문장. 그 업종을 모르는 사람 기준으로 쓴다.
2. **무슨 일이 있었고 그것을 어떻게 읽었는가** 한두 문장. 숫자는 한두 개만.
3. **표 세 줄** — `산다` / `판다(손절)` / `계획을 버린다`. 9절 표에서 가져오고 **가격을 새로 쓰지 않는다.**
4. 기준 종가와 기준 종가일, 그리고 아래가 근거라는 한 줄.

표 포함 **900자 안**에 끝낸다. 여기서 못 쓰는 조건은 9절에 있으니 옮겨 담지 않는다.
이 절의 가격은 9절 표의 값과 **글자까지 같아야 한다** — 검사기가 대조한다.

**1절은 그날의 사건과 그것이 만드는 질문만 펼쳐 둔다.** 제외 목록 확인, 시장 판정(짝수일/홀수일),
후보 점수표는 **에이전트가 자기 절차를 지켰다는 기록**이지 회사에 대한 분석이 아니다. 지우지는 않는다 —
무인 실행이 7일 규칙을 지켰는지 되짚을 방법이 이것뿐이다. 대신 1절 안의 `<details>` 한 블록
("선정 과정")에 모아 접는다.

**③ 접기** — `## 출처`와 `## 방법론 · 재현`은 `##` 제목을 남기고 본문만 접는다
(제목이 사이드바에 남아야 찾을 수 있다).

```markdown
## 출처

<details>
<summary>공시·데이터 출처 23건 — 펼치기</summary>

- [반기보고서 (2026.06) / 2026-08-14] https://...

</details>
```

접는 것은 이 셋뿐이다. **강세·약세 논거나 매매 조건을 접지 않는다** — 불편해도 읽어야 하는 것이다.

### 분석 전에 배경을 먼저 준다

처음 읽는 사람에게 부족한 것은 분석이 아니라 **배경**이다. 부문별 매출 표를 바로 들이대면
그 숫자가 무엇을 뜻하는지 알 수 없다. 그래서 2절은 `## 2. 산업과 사업 구조`이고 소절 둘로 나뉜다.

**`### 이 산업은 어떻게 돌아가는가`** — 세 조각이다.

1. **산업의 구조** — 무엇을 팔아 돈을 버는 산업인가, 수요를 결정하는 것은 무엇인가,
   진입장벽·규제·경기 민감도는 무엇인가. 밑줄 그은 항목은 **공시에 그대로 있다**:
   한국은 사업보고서 `II. 사업의 내용`(산업의 특성 / 산업의 성장성 / 경기변동의 특성),
   미국은 10-K `Item 1. Business`(Competition / Customers / Raw Materials / Seasonality)와 `Item 1A`.
2. **경쟁 구도와 점유율** — 공시된 경쟁사·점유율. 점유율은 대개 **회사 자신의 주장**이므로
   `(회사 주장, <문서>)`를 붙인다. 공시에 없으면 "확인 불가"로 남긴다.
3. **이 산업의 말** — 그 업종에서만 쓰는 용어 3~5개를 한 줄씩 푼다. 본문에서 두 번 풀지 않는다.

**`### 이 회사는 그 안에서 무엇을 하는가`** — 부문·지역별 매출과 고객. 기존 2절 내용이 여기 온다.

산업 서술에도 같은 철칙이 걸린다.

- **기억에서 쓰지 않는다.** 산업 설명도 공시에서 가져오고 어느 문서의 어느 항목인지 적는다.
  2차 자료의 시장 규모·성장률을 옮기지 않는다.
- **회사의 전망 문장을 옮기지 않는다.** 공시의 산업 서술에는 "향후 성장할 것으로 전망됩니다" 같은
  문장이 섞여 있다. 구조·규제·사실만 가져오고 전망은 버린다 — 옮기면 금지된 예측 서술이 된다.
- **산업이 좋다는 것을 논거로 쓰지 않는다.** 배경은 8절 논거가 아니다. 논거로 쓰려면
  그 변수가 이 회사 숫자에 어떻게 닿는지를 공시에서 찾아야 한다(거시와 같은 규칙).

---

## 커밋

- 메시지: `brief(YYYY-MM-DD): <종목명> — <한 줄 요약>`
- 보고서 1건 = 커밋 1건. 검증 미통과 상태로 커밋하지 않는다.
- 브랜치·PR을 만들지 않는다. 검증을 통과했으면 `main`에 바로 커밋하고 푸시한다
  (푸시하면 사이트가 자동 배포된다 — 공개되는 문서다).
- 저장소 문서(AGENTS.md, 사이트 설정 등) 수정은 `docs:` 프리픽스로 분리한다.
- **푸시 후 배포를 확인한다.** 푸시가 곧 게시는 아니다 — `deploy` 잡이 `waiting`에서 멈추는 일이 있다.
  확인·복구 절차는 `daily-brief` 스킬 9단계의 "배포 확인"에 있다.

---

## 하지 않는 것

- **타인에게 권하는 문장** — 이 저장소는 주인 본인의 판단 기준을 기록하는 곳이다. "추천", "지금 사야 한다"는 쓰지 않는다.
- **예측 서술** — "오를 것으로 보인다", "가능성이 높다". 조건문으로 바꿔 쓰거나 지운다.
- **무효화 조건 없는 진입 조건** — 파는 조건을 못 쓰겠으면 사는 조건도 쓰지 않는다.
- **쓸 수 없는 진입 조건을 레벨로 채우기** — 진입가를 가격으로 쓸 수 없는 날은 1차 진입 행을
  비우고 스탠스를 `관망`·`보류`로 둔다. 무효화 조건은 그래도 쓴다. 매일 `진입 대기`가 반복되면
  그건 판단이 아니라 관성이다
- 확인되지 않은 숫자를 근사치로 채워 넣기
- 공시가 없는 사안에 대해 "~일 것으로 보인다"로 서술 확장
- 가격 움직임을 펀더멘털 논거로 쓰기 — "많이 빠졌으니 싸다"
- **배수 분위를 확률로 읽기** — "25분위였으니 오른다". 밴드는 지나간 구간을 잰 숫자이고,
  하는 일은 9절에 `[공시]` 근거의 가격을 공급하는 것 하나다. 실적 발표 직후에는 배수가 내려간 것이
  가격이 아니라 **분모가 커진 결과**일 수 있으므로 그 구분을 함께 적는다
- **연결고리 없는 거시 서사** — 회사 공시에서 그 변수를 찾지 못했으면 쓰지 않는다. 거시는 논거가
  아니라 논거의 트리거·기각 신호이고, 관측일과 조회일을 함께 적는다(스킬 4단계 "거시 지표")
- 매수 서사에 맞추려고 약세 논거를 빼기 — 양쪽 논거는 항상 함께 쓴다
- 지표 파라미터를 종목마다 유리하게 바꾸기

---

*이 문서의 작업 방식은 [claude-code-best-practices.md](claude-code-best-practices.md)
(Claude Code 공식 문서 정리)의 원칙을 이 프로젝트에 맞게 적용한 것이다.
해당 문서는 사람이 읽는 참고 자료이며, 에이전트가 매 세션 읽을 필요는 없다.*
